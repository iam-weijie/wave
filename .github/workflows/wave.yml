name: Wave

on:
  schedule:
    - cron: '*/5 * * * *' # Run every 5 minutes
  push:
    branches:
      - main

jobs:
  wave:
    runs-on: ubuntu-latest
    steps:
      - name: Display waving hand emoji
        run: echo "👋"
